import styled from "styled-components";
import { ColorToken } from "../../theme";

/** Basically a really simple separator. */
export const Hr = styled.div`
  border-bottom: 1px solid ${ColorToken.Black100};
`;
